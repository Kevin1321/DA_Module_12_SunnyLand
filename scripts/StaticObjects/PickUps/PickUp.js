class PickUp extends GameObject {
    constructor(positionX, positionY, sizeX, sizeY) {
        super(positionX, positionY, sizeX, sizeY);
    }

    OnTick(frame, deltaTime) {
        super.OnTick(frame, deltaTime);
    }

    //handle collision here => event callback!
}